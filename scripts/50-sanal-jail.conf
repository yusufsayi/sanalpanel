# SanalPanel jailed shell — sanal-ssh grubundaki hosting hesapları chroot'lanır
Match Group sanal-ssh
    ChrootDirectory /home/jails/%u
    X11Forwarding no
    AllowTcpForwarding no
    AllowAgentForwarding no
    PermitTunnel no
